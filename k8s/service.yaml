apiVersion: v1
kind: Service
metadata:
  name: iris-api
  namespace: {{NAMESPACE}}
spec:
  type: LoadBalancer
  selector:
    app: iris-api
  ports:
    - port: {{APP_PORT}}
      targetPort: {{APP_PORT}}
      protocol: TCP
