apiVersion: apps/v1
kind: Deployment
metadata:
  name: iris-classifier-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iris-classifier-service
  template:
    metadata:
      labels:
        app: iris-classifier-service
    spec:
      serviceAccountName: telemetry-access 
      containers:
      - name: fastapi-container
        # This image is replaced by the CI/CD workflow before apply
        image: IMAGE_WILL_BE_REPLACED_BY_CICD
        ports:
        - containerPort: 8200
        readinessProbe:
          httpGet:
            path: /ready_check
            port: 8200
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /live_check
            port: 8200
          initialDelaySeconds: 15
          periodSeconds: 20
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"